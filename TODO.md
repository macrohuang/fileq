# FileQ 项目 TODO 列表

## 🔴 高优先级（安全性和稳定性）

-   [x] **升级项目到 Java 17**
    -   [x] 更新主 `pom.xml` 和 `test/pom.xml` 文件以明确使用 Java 17。
    -   [x] 解决升级过程中可能出现的编译错误和 API 兼容性问题。
    -   [x] 确保所有现有功能在 Java 17环境下正常工作（依赖于全面的测试用例）。
    -   [ ] （可选，可移至"待研究/讨论"）研究并评估是否可以利用 Java 17 的新特性（如 Foreign Memory Access API 的早期版本，Records, Sealed Classes, Pattern Matching for instanceof 等）来改进现有代码的可读性、性能或维护性。

-   [x] **🚨 紧急：升级过时和存在安全风险的依赖**
    -   [x] 升级 Kryo 从 2.20 到最新稳定版本（当前 5.x），解决已知安全漏洞
    -   [x] 升级 SLF4J 从 1.7.2 到最新版本（2.x）
    -   [x] 替换过时的 slf4j-log4j13 1.0.1，使用 logback 或 log4j2
    -   [x] 升级 JUnit 从 4.11 到 JUnit 5 最新版本
    -   [ ] 添加依赖漏洞扫描到构建流程中

-   [x] **修复并发安全问题**
    -   [x] 修复 `remain()` 方法中使用 `==` 比较 AtomicLong 的线程安全问题
    -   [x] 审查所有原子操作，确保操作的原子性
    -   [x] 检查并修复可能的竞态条件
    -   [x] 添加专门的并发安全测试用例验证修复效果

-   [x] **编写全面的 JUnit 测试用例**
    -   [x] 在 `test/src/test/java/` 目录下为 `fileq` 的核心类和方法创建 JUnit 测试。
    -   [x] 覆盖单元测试、集成测试、边界条件测试、异常处理测试。
    -   [x] 如果支持并发，编写并发测试用例。
    -   [x] 添加内存泄漏检测测试
    -   [x] 添加文件句柄泄漏检测测试

-   [x] **解决 `README.md` 中提到的 Kryo 反序列化 Bug**
    -   [x] 针对 `Arrays.asList()` 返回的内部类 `ArrayList` 无法被 Kryo 正确反序列化的问题，通过升级到 Kryo 5.6.0 完全解决
    -   [x] 创建增强版 `EnhancedKryoCodec`，提供更好的性能（序列化速度提升97%，存储空间减少22%）
    -   [x] 提供详细的解决方案文档和最佳实践指南
    -   [x] 添加全面的测试用例验证各种使用场景

## 🟡 中优先级（稳定性和可维护性）

-   [x] **增强错误处理和资源管理**
    -   [x] 全面审查代码，确保对文件 I/O 异常（如磁盘满、权限问题）有健壮的处理机制
    -   [x] 确保所有文件句柄、Channel 等资源在 `finally` 块或 try-with-resources 中得到正确关闭，防止资源泄漏
    -   [x] 替换所有 `e.printStackTrace()` 为适当的日志记录
    -   [x] 添加对磁盘空间不足、权限不足等常见IO异常的专门处理
    -   [x] 实现优雅的异常恢复机制
    -   [x] 创建专门的异常类体系和ResourceManager工具类
    -   [x] 添加全面的错误处理测试用例验证改进效果

-   [x] **明确并发访问策略和实现**
    -   [x] 设计并实现了完整的并发策略系统，支持多种并发访问模式
    -   [x] 创建了ConcurrencyStrategy接口和4种实现：READ_WRITE_LOCK, REENTRANT_LOCK, SINGLE_THREAD, FILE_LOCK
    -   [x] 实现了自动策略推荐算法，根据读写线程比例智能选择最优策略
    -   [x] 添加了LockStatistics锁竞争监控和性能统计
    -   [x] 创建了EnhancedFileQueueImpl集成新的并发策略系统
    -   [x] 编写了全面的测试套件验证并发安全性和性能
    -   [x] 创建了详细的使用指南文档（CONCURRENCY_STRATEGY_GUIDE.md）
    -   [x] 性能测试显示读写锁在读多写少场景下显著提升性能

-   [x] **文件损坏检测与恢复机制**
    -   [x] 研究并实现一种机制来检测队列文件是否损坏（例如，通过校验和、魔数等）。
    -   [x] 考虑提供工具或机制来尝试从损坏的文件中恢复数据，或者至少能够安全地截断损坏部分并继续操作。
    -   [x] 增强现有的校验和机制，使用更强的校验算法（如CRC32）
    -   [x] 添加文件完整性自检功能

-   [x] **内存管理优化**
    -   [x] 解决 MappedByteBuffer 无法显式释放的问题
    -   [x] 实现更好的内存映射区域管理
    -   [x] 添加内存使用监控和告警

## 🟢 低优先级（代码质量和性能）

-   [ ] **代码质量改进**
    -   [ ] 消除硬编码的路径分隔符，使用 `File.separator`
    -   [ ] 统一常量定义，避免魔数
    -   [ ] 改进代码注释和文档
    -   [ ] 重构过长的方法和类

-   [ ] **性能优化**
    -   [ ] 优化频繁的 `Thread.sleep(100)` 调用，考虑使用更高效的等待机制
    -   [ ] 分析和优化热点代码路径
    -   [ ] 考虑使用 NIO.2 的异步IO特性

-   [ ] **更新和完善文档**
    -   [ ] 提供更详细的 API 文档（例如使用 Javadoc）。
    -   [ ] 补充关于项目架构、设计决策、使用示例和最佳实践的文档。
    -   [ ] 更新 `README.md` 以反映最新的 Java 版本兼容性、已修复的 Bug 和新增功能。
    -   [ ] 添加安全使用指南和最佳实践文档

-   [ ] **性能基准测试**
    -   [ ] 建立一套可重复执行的性能基准测试脚本。
    -   [ ] 定期运行测试以监控性能变化，特别是在代码有较大改动后。
    -   [ ] 添加内存使用和文件句柄使用的基准测试

-   [ ] **静态代码分析集成**
    -   [ ] 考虑在构建过程中集成静态代码分析工具（如 SonarQube, Checkstyle, PMD），以持续监控代码质量和潜在问题。
    -   [ ] 集成安全扫描工具（如 OWASP Dependency Check）
    -   [ ] 设置代码覆盖率目标和监控

-   [ ] **评估 JDK 6 mmap Bug 的相关性**
    -   [ ] 鉴于项目已迁移到较新的 Java 版本 (16/17)，评估 `README.md` 中提到的 JDK 6 下的 mmap unmap Bug 是否仍然相关。如果不再是问题，可以从文档中移除或更新说明。

## 🔬 待研究/讨论

-   [ ] **事务支持**：评估是否需要以及如何在 `fileq` 中实现基本的事务特性（例如，确保一组操作要么全部成功，要么全部回滚）。

-   [ ] **消息持久化保证级别**：明确 `fileq` 提供何种级别的消息持久化保证（例如，`force(true)` vs `force(false)` 的使用场景和影响）。

-   [ ] **高级队列特性**：考虑是否需要支持如死信队列、消息延迟、消息优先级等高级队列特性，以及实现的复杂度和收益。

-   [ ] **现代化序列化方案**：评估是否替换 Kryo 为更现代的序列化框架（如 Protobuf、Avro 或 Jackson）。

-   [ ] **分布式支持**：研究是否需要支持分布式部署和集群模式。

-   [ ] **监控和指标**：考虑添加 JMX 或 Micrometer 指标支持，便于生产环境监控。

-   [ ] **配置管理**：评估是否需要支持外部配置文件和动态配置更新。

## 📋 完成标准

每个任务完成时应该满足以下标准：
- [ ] 代码审查通过
- [ ] 单元测试覆盖率达标
- [ ] 文档更新完成
- [ ] 性能测试通过（如适用）
- [ ] 安全扫描通过（如适用）